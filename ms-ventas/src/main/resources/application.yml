spring:
  application:
    name: ms-ventas
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/db_ventas?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        physical_naming_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        format_sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog.sql

logging:
  level:
    liquibase: DEBUG
 
server:
  port: 8089
 
api:
  dueno:
    url: http://host.docker.internal:8082
  mascota:
    url: http://host.docker.internal:8083
 
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true